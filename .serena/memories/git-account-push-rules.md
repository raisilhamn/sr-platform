When pushing to GitHub from these directories, always use the `raisilhamn` account (switch via `gh auth switch --user raisilhamn`), then switch back to `raisadibayu` after:

- `/home/rais/Downloads/pribadi/`
- `/home/rais/Downloads/CPNS/sr-app/`
